<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">YelpCamp</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <nav class="navbar-nav">
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link <%= currentPath === '/campgrounds' ? 'active' : '' %>" href="/campgrounds">Campgrounds</a>
                <a class="nav-link <%= currentPath === '/campgrounds/map' ? 'active' : '' %>" href="/campgrounds/map">Map</a>
                <% if (currentUser) {%>
                <a class="nav-link <%= currentPath === '/campgrounds/new' ? 'active' : '' %>" href="/campgrounds/new">New Campground</a>
                <% } %>
            </nav>
            <nav class="navbar-nav ms-auto">
                <% if (!currentUser) {%>
                <a href="/login<%= typeof redirect_url !== 'undefined' ? `?redirect_url=${redirect_url}` : '' %>"
                    class="nav-link">Login</a>
                <a href="/register" class="nav-link">Register</a>
                <% } else { %>
                <a href="/logout" class="nav-link">Logout</a>
                <% } %>
            </nav>
        </div>
    </div>
</nav>